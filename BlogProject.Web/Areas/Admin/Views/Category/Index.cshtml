@model List<CategoryDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="container-xxl flex-grow-1 container-p-y">

    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Kategori Yönetimi / </span> Kategoriler
    </h4>

    <div class="card">
        <h5 class="card-header"> Table Basic</h5>
        <div class="table-responsive text-nowrap m-3">

            <a class="btn btn-primary mb-3" asp-area="Admin" asp-controller="Category" asp-action="Add">Kategori Ekle <i class="bx bx-plus"></i></a>

            <table class="table table-bordered" id="categoryDataTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>Kategori Adı</th>
                        <th>Oluşturan</th>
                        <th>Oluşturma Tarihi</th>
                        <th>Durum</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th>Kategori Adı</th>
                        <th>Oluşturan</th>
                        <th>Oluşturma Tarihi</th>
                        <th>Durum</th>
                        <th>İşlemler</th>
                    </tr>
                </tfoot>
                <tbody class="table-border-bottom-0">
                    @foreach (var item in Model)
                    {
                        <tr name="@item.Id">
                            <td>@item.Name</td>
                            <td>@item.CreatedBy</td>
                            <td>@item.CreatedDate.ToString("dd/MM/yyyy")</td>
                            <td>@(item.IsDeleted ? "Aktif Değil" : "Aktif")</td>
                            <td>
                                <a class="btn btn-warning btn-sm" data-id="@item.Id" asp-area="Admin" asp-controller="Category" asp-action="Update" asp-route-categoryId="@item.Id">
                                    <i class="bx bx-edit"></i>
                                </a>
                                <a class="btn btn-danger btn-sm" data-id="@item.Id" asp-area="Admin" asp-controller="Category" asp-action="Delete" asp-route-categoryId="@item.Id">
                                    <i class="bx bx-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

</div>

@section Scripts {
    <script src="~/js/categoryindex.js"></script>
}
